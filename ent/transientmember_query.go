// Code generated by ent, DO NOT EDIT.

package ent

import (
	"backend_golang/ent/predicate"
	"backend_golang/ent/transientmember"
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// TransientMemberQuery is the builder for querying TransientMember entities.
type TransientMemberQuery struct {
	config
	ctx        *QueryContext
	order      []transientmember.OrderOption
	inters     []Interceptor
	predicates []predicate.TransientMember
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the TransientMemberQuery builder.
func (tmq *TransientMemberQuery) Where(ps ...predicate.TransientMember) *TransientMemberQuery {
	tmq.predicates = append(tmq.predicates, ps...)
	return tmq
}

// Limit the number of records to be returned by this query.
func (tmq *TransientMemberQuery) Limit(limit int) *TransientMemberQuery {
	tmq.ctx.Limit = &limit
	return tmq
}

// Offset to start from.
func (tmq *TransientMemberQuery) Offset(offset int) *TransientMemberQuery {
	tmq.ctx.Offset = &offset
	return tmq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (tmq *TransientMemberQuery) Unique(unique bool) *TransientMemberQuery {
	tmq.ctx.Unique = &unique
	return tmq
}

// Order specifies how the records should be ordered.
func (tmq *TransientMemberQuery) Order(o ...transientmember.OrderOption) *TransientMemberQuery {
	tmq.order = append(tmq.order, o...)
	return tmq
}

// First returns the first TransientMember entity from the query.
// Returns a *NotFoundError when no TransientMember was found.
func (tmq *TransientMemberQuery) First(ctx context.Context) (*TransientMember, error) {
	nodes, err := tmq.Limit(1).All(setContextOp(ctx, tmq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{transientmember.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (tmq *TransientMemberQuery) FirstX(ctx context.Context) *TransientMember {
	node, err := tmq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first TransientMember ID from the query.
// Returns a *NotFoundError when no TransientMember ID was found.
func (tmq *TransientMemberQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = tmq.Limit(1).IDs(setContextOp(ctx, tmq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{transientmember.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (tmq *TransientMemberQuery) FirstIDX(ctx context.Context) int {
	id, err := tmq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single TransientMember entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one TransientMember entity is found.
// Returns a *NotFoundError when no TransientMember entities are found.
func (tmq *TransientMemberQuery) Only(ctx context.Context) (*TransientMember, error) {
	nodes, err := tmq.Limit(2).All(setContextOp(ctx, tmq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{transientmember.Label}
	default:
		return nil, &NotSingularError{transientmember.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (tmq *TransientMemberQuery) OnlyX(ctx context.Context) *TransientMember {
	node, err := tmq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only TransientMember ID in the query.
// Returns a *NotSingularError when more than one TransientMember ID is found.
// Returns a *NotFoundError when no entities are found.
func (tmq *TransientMemberQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = tmq.Limit(2).IDs(setContextOp(ctx, tmq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{transientmember.Label}
	default:
		err = &NotSingularError{transientmember.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (tmq *TransientMemberQuery) OnlyIDX(ctx context.Context) int {
	id, err := tmq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of TransientMembers.
func (tmq *TransientMemberQuery) All(ctx context.Context) ([]*TransientMember, error) {
	ctx = setContextOp(ctx, tmq.ctx, ent.OpQueryAll)
	if err := tmq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*TransientMember, *TransientMemberQuery]()
	return withInterceptors[[]*TransientMember](ctx, tmq, qr, tmq.inters)
}

// AllX is like All, but panics if an error occurs.
func (tmq *TransientMemberQuery) AllX(ctx context.Context) []*TransientMember {
	nodes, err := tmq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of TransientMember IDs.
func (tmq *TransientMemberQuery) IDs(ctx context.Context) (ids []int, err error) {
	if tmq.ctx.Unique == nil && tmq.path != nil {
		tmq.Unique(true)
	}
	ctx = setContextOp(ctx, tmq.ctx, ent.OpQueryIDs)
	if err = tmq.Select(transientmember.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (tmq *TransientMemberQuery) IDsX(ctx context.Context) []int {
	ids, err := tmq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (tmq *TransientMemberQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, tmq.ctx, ent.OpQueryCount)
	if err := tmq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, tmq, querierCount[*TransientMemberQuery](), tmq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (tmq *TransientMemberQuery) CountX(ctx context.Context) int {
	count, err := tmq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (tmq *TransientMemberQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, tmq.ctx, ent.OpQueryExist)
	switch _, err := tmq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (tmq *TransientMemberQuery) ExistX(ctx context.Context) bool {
	exist, err := tmq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the TransientMemberQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (tmq *TransientMemberQuery) Clone() *TransientMemberQuery {
	if tmq == nil {
		return nil
	}
	return &TransientMemberQuery{
		config:     tmq.config,
		ctx:        tmq.ctx.Clone(),
		order:      append([]transientmember.OrderOption{}, tmq.order...),
		inters:     append([]Interceptor{}, tmq.inters...),
		predicates: append([]predicate.TransientMember{}, tmq.predicates...),
		// clone intermediate query.
		sql:  tmq.sql.Clone(),
		path: tmq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		TransientMemberID string `json:"transient_member_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.TransientMember.Query().
//		GroupBy(transientmember.FieldTransientMemberID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (tmq *TransientMemberQuery) GroupBy(field string, fields ...string) *TransientMemberGroupBy {
	tmq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &TransientMemberGroupBy{build: tmq}
	grbuild.flds = &tmq.ctx.Fields
	grbuild.label = transientmember.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		TransientMemberID string `json:"transient_member_id,omitempty"`
//	}
//
//	client.TransientMember.Query().
//		Select(transientmember.FieldTransientMemberID).
//		Scan(ctx, &v)
func (tmq *TransientMemberQuery) Select(fields ...string) *TransientMemberSelect {
	tmq.ctx.Fields = append(tmq.ctx.Fields, fields...)
	sbuild := &TransientMemberSelect{TransientMemberQuery: tmq}
	sbuild.label = transientmember.Label
	sbuild.flds, sbuild.scan = &tmq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a TransientMemberSelect configured with the given aggregations.
func (tmq *TransientMemberQuery) Aggregate(fns ...AggregateFunc) *TransientMemberSelect {
	return tmq.Select().Aggregate(fns...)
}

func (tmq *TransientMemberQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range tmq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, tmq); err != nil {
				return err
			}
		}
	}
	for _, f := range tmq.ctx.Fields {
		if !transientmember.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if tmq.path != nil {
		prev, err := tmq.path(ctx)
		if err != nil {
			return err
		}
		tmq.sql = prev
	}
	return nil
}

func (tmq *TransientMemberQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*TransientMember, error) {
	var (
		nodes = []*TransientMember{}
		_spec = tmq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*TransientMember).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &TransientMember{config: tmq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, tmq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (tmq *TransientMemberQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := tmq.querySpec()
	_spec.Node.Columns = tmq.ctx.Fields
	if len(tmq.ctx.Fields) > 0 {
		_spec.Unique = tmq.ctx.Unique != nil && *tmq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, tmq.driver, _spec)
}

func (tmq *TransientMemberQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(transientmember.Table, transientmember.Columns, sqlgraph.NewFieldSpec(transientmember.FieldID, field.TypeInt))
	_spec.From = tmq.sql
	if unique := tmq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if tmq.path != nil {
		_spec.Unique = true
	}
	if fields := tmq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, transientmember.FieldID)
		for i := range fields {
			if fields[i] != transientmember.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := tmq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := tmq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := tmq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := tmq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (tmq *TransientMemberQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(tmq.driver.Dialect())
	t1 := builder.Table(transientmember.Table)
	columns := tmq.ctx.Fields
	if len(columns) == 0 {
		columns = transientmember.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if tmq.sql != nil {
		selector = tmq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if tmq.ctx.Unique != nil && *tmq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range tmq.predicates {
		p(selector)
	}
	for _, p := range tmq.order {
		p(selector)
	}
	if offset := tmq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := tmq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// TransientMemberGroupBy is the group-by builder for TransientMember entities.
type TransientMemberGroupBy struct {
	selector
	build *TransientMemberQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (tmgb *TransientMemberGroupBy) Aggregate(fns ...AggregateFunc) *TransientMemberGroupBy {
	tmgb.fns = append(tmgb.fns, fns...)
	return tmgb
}

// Scan applies the selector query and scans the result into the given value.
func (tmgb *TransientMemberGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, tmgb.build.ctx, ent.OpQueryGroupBy)
	if err := tmgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*TransientMemberQuery, *TransientMemberGroupBy](ctx, tmgb.build, tmgb, tmgb.build.inters, v)
}

func (tmgb *TransientMemberGroupBy) sqlScan(ctx context.Context, root *TransientMemberQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(tmgb.fns))
	for _, fn := range tmgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*tmgb.flds)+len(tmgb.fns))
		for _, f := range *tmgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*tmgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := tmgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// TransientMemberSelect is the builder for selecting fields of TransientMember entities.
type TransientMemberSelect struct {
	*TransientMemberQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (tms *TransientMemberSelect) Aggregate(fns ...AggregateFunc) *TransientMemberSelect {
	tms.fns = append(tms.fns, fns...)
	return tms
}

// Scan applies the selector query and scans the result into the given value.
func (tms *TransientMemberSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, tms.ctx, ent.OpQuerySelect)
	if err := tms.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*TransientMemberQuery, *TransientMemberSelect](ctx, tms.TransientMemberQuery, tms, tms.inters, v)
}

func (tms *TransientMemberSelect) sqlScan(ctx context.Context, root *TransientMemberQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(tms.fns))
	for _, fn := range tms.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*tms.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := tms.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
